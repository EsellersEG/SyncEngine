# Sync-Engine on Railway

[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build:all"
startCommand = "npm start"

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5

[[services]]
name = "web"
